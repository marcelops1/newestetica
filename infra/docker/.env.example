# Ambiente local de desenvolvimento — copie para .env (ignorado pelo git).
# Valores fictícios de desenvolvimento; nunca usar segredos reais aqui.

# Postgres
POSTGRES_USER=newestetica
POSTGRES_PASSWORD=changeme-dev
POSTGRES_DB=newestetica
POSTGRES_PORT=5432

# Keycloak (modo dev)
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme-dev
KEYCLOAK_PORT=8080

# Backend
BACKEND_PORT=3001
# Stack local serve /docs e /docs-json (produção exige a flag explícita; ver 03-seguranca.md §8)
SWAGGER_ENABLED=true
