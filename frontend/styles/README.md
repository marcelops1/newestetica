# styles/ — Design System e tokens

Fonte única de estilo: CSS variables + tema Tailwind (cores, tipografia, espaçamentos, radius, sombras, bordas) conforme `docs/06-design-system.md`.
Regra: nenhum componente usa valor literal divergente dos tokens.
