# app/ — Rotas e páginas (Next.js App Router)

Cada rota do site público e do painel vive aqui (`page.tsx`, `layout.tsx`).
Componentes e lógica ficam em `components/`, `features/` e `lib/` — nunca direto nas páginas além da composição.
