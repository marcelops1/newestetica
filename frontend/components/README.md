# components/ — Componentes reutilizáveis

Blocos genéricos de UI (botões, cards, seção, cabeçalho de seção, CTA) usados por várias telas.
Regras: composição sobre configuração, sem regra de negócio, estilos só via tokens de `styles/`.
