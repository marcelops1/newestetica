# features/ — Funcionalidades por domínio

Uma pasta por domínio do MVP (ex.: `catalogo/`, `agendamento/`, `depoimentos/`), cada uma com seus componentes de seção, tipos e acesso a dados.
Regra: apresentação consome a camada de dados via interfaces — nunca importa mocks diretamente.
