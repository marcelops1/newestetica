{
  "name": "newestetica",
  "private": true,
  "version": "0.0.1",
  "description": "Sistema para clínica de estética da Fabiana Rosa - foco em mulheres 40+",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "prepare": "husky",
    "dev": "pnpm --filter frontend dev",
    "lint": "node --check lint-staged.config.mjs && pnpm --filter frontend --filter contracts lint",
    "format": "pnpm --filter frontend exec prettier --check ../lint-staged.config.mjs && pnpm --filter frontend --filter contracts format",
    "typecheck": "pnpm --filter frontend --filter contracts typecheck",
    "test": "pnpm --filter frontend --filter contracts test",
    "build": "pnpm --filter frontend build",
    "openspec": "openspec",
    "infra:up": "docker compose -f infra/docker/docker-compose.yml up -d --wait",
    "infra:down": "docker compose -f infra/docker/docker-compose.yml down --remove-orphans"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@fission-ai/openspec": "1.12.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1"
  }
}
