## 1. Contrato/schema e submissão mockada (TDD estrito — docs/07 §13: unitários com edge cases + contrato/schema com apoio da skill `api-and-interface-design`)

- [x] 1.1 RED: escrever `frontend/lib/__tests__/quote.test.ts` testando o contrato da fronteira UI ↔ submissão mockada (payload válido com nome/WhatsApp/procedimento/mensagem passa no schema; payload malformado — campo ausente, tipo errado, procedimento desconhecido — retorna erro acolhedor sem vazar detalhes) e o comportamento de `submitQuoteRequest` (sucesso com tratamento solicitado, default acolhedor quando vazio, `forceError` com mensagem sem culpa, nenhuma chamada de rede via spy de `fetch`) e verificar que a suite falha (módulo ainda inexistente) — RED comprovado: `Cannot find module '../quote'`
- [x] 1.2 GREEN: implementar `frontend/lib/quote.ts` (`QuoteInput`/`QuoteResult`, schema/validador do payload desenhado com apoio da skill `api-and-interface-design`, `submitQuoteRequest` espelhando `submitBookingRequest`: 100% local, delay mockado, `forceError` só para demonstração/testes) e verificar que o teste da task 1.1 passa, os testes existentes (`booking.test.ts`, `data.test.ts`) seguem passando e a cobertura fica acima de 80% — GREEN comprovado: 26/26 testes (quote+booking+data); cobertura completa nos gates da task 4.1

## 2. Segurança OWASP de entrada (task dedicada — gatilho docs/07 §7: formulário = entrada de usuário real; docs/07 §13: segurança orientada a OWASP)

- [x] 2.1 RED: estender `frontend/lib/__tests__/quote.test.ts` (ou novo `quote-security.test.ts`) com casos maliciosos — `<script>alert(1)</script>`, `<img src=x onerror=...>`, `&`, aspas em nome/mensagem, WhatsApp com letras/máscara incompleta, mensagem longa — exigindo tratamento como texto (sem execução/interpretação de HTML), validação nome ≥2 caracteres e WhatsApp ≥10 dígitos com mensagens acolhedoras em português, e verificar que os novos testes falham — RED comprovado: 8/12 falhando (`validateQuoteFields` inexistente + fronteira aceitando payload inválido)
- [x] 2.2 GREEN: implementar o hardening na validação/escaping de `lib/quote.ts` (e no reaproveitamento da validação do `BookingModal` na página) e verificar que os testes da task 2.1 passam, que `grep -r "innerHTML\|dangerouslySetInnerHTML" frontend/` retorna vazio na nova superfície, e que a cobertura segue acima de 80% — GREEN comprovado: 50/50 testes, grep vazio, cobertura 97,95% stmts / 92,42% branches / 100% funcs / 97,77% lines

## 3. Página /orcamento e fluxo crítico (lógica testada nas seções 1–2; montagem de UI por gates — sem testing-library no projeto, lacuna registrada no design)

- [ ] 3.1 Implementar `frontend/features/quote/QuotePage.tsx` (client component padrão `AboutPage`: formulário nome/WhatsApp/select via `getTreatmentOptions()`/mensagem opcional reaproveitando microcopy, classes e nota LGPD do `BookingModal`; 4 estados ocioso/enviando/sucesso/erro com dados preservados e nova tentativa; confirmação acolhedora sem prometer valores fechados; mobile-first 16px+/toque 44px+, `aria-invalid`/`aria-describedby`) e `frontend/app/orcamento/page.tsx` com metadata em tom acolhedor, e verificar que lint, format, typecheck, testes e build passam com `/orcamento` prerenderizada
- [ ] 3.2 Verificar o fluxo crítico preencher → enviar → confirmação pelos gates disponíveis (build prerenderiza a rota, typecheck, testes da lib verdes, revisão manual do fluxo sucesso/erro com dados preservados) e verificar que nenhum fetch/XHR foi introduzido (spy de `fetch` nos testes + `grep` por `fetch(`/`axios` na nova superfície, exceto o spy)

## 4. Gates, revisão de segurança e backlog (Definition of Done docs/07 §6; revisão `security-and-hardening` obrigatória pelo gatilho §7)

- [ ] 4.1 Rodar quality gates (`lint`, `format`, `typecheck`, testes com cobertura >80%, `build`) e `openspec validate`, registrar em `verification.md` a revisão com `security-and-hardening` contra `docs/security/03-seguranca.md` (entrada de usuário, XSS/escaping, LGPD, sem dado real) e a revisão com `code-review-and-quality`, com as dispensas da seção 13 (mutation, E2E, carga) justificadas como no design, e verificar que tudo está verde
- [ ] 4.2 Atualizar `docs/product/08-backlog-produto.md` (Use Case 1.7.1 — Pedir orçamento personalizado: status Não iniciado → Em andamento/Concluído conforme o apply, sem alterar nenhum outro Use Case) e verificar que o trecho do UC 1.7.1 reflete o que foi entregue neste change
