## 1. Sincronizar diagramas (mudança sem comportamento — exceção docs/07 §4 registrada aqui)

> Exceção aplicável: edição só de Markdown de arquitetura; verificação por leitura + `ls` + validate, sem ciclo RED.

- [x] 1.1 Atualizar `c2-container.md` (rotas reais no contêiner Frontend) e verificar que as 4 rotas listadas existem em `frontend/app`
- [x] 1.2 Atualizar `c3-component.md` (features reais + rotas) e verificar que cada pasta citada existe no repositório

## 2. Verificação e archive

- [x] 2.1 Rodar quality gates e `openspec validate` e verificar que tudo passa sem tocar em código
- [x] 2.2 Registrar verificação de segurança (docs puros: sem gatilho) e arquivar com specs sincronizadas
