## 1. Stryker no backend (test-first)

- [x] 1.1 Executar `pnpm --filter backend exec stryker --version` e constatar que falha (binário inexistente) — RED. Verificação: saída de erro colada na task. Execução: `Command "stryker" not found` (RED real)
- [x] 1.2 Instalar `@stryker-mutator/core` + `vitest-runner` (mesmas versões do frontend), criar `backend/stryker.config.mjs` (escopo `src/scheduling/**`, exclusões de teste/gerado/wiring equivalentes, mesmos thresholds) e script `mutation`; `stryker --version` passa — GREEN. Verificação: binário resolve e config carrega. Execução: `stryker --version` → 10.0.0. **Achado que mudou o desenho:** o `vitest-runner@10.0.0` (última versão) não ativa mutantes no `vitest@5` (hooks API mudou; o setup escreve cobertura em `suite.meta`, que não chega) — resultado silencioso de 0% com todos os mutantes "sobrevivendo". Fallback aplicado conforme design decisão 4: `testRunner: "command"` (exit code) + `vitest.mutation.config.ts` single-project (o runner também não lida com os `projects` do vitest: IDs de teste colidem no merge de cobertura) + `plugins` explícito (discovery falha no layout pnpm) + `concurrency: 1` (integração compartilha o banco de teste)
- [x] 1.3 Executar o mutation no escopo do Agendamento com o banco de teste no ar (fallback da decisão 4 do design se o plugin não suportar `projects`) e registrar score + tempo real em `verification.md` — GREEN. Verificação: relatório Stryker completo colado/resumido no registro. Execução: **score real 82,89%** na 1ª medição (189/228 mortos, 12m35s com `--bail=1`); após triagem, **85,53%** (2ª medição, 10m39s); re-medição final com os testes de fronteira em andamento. Tempos e tabela por arquivo em `verification.md`
- [x] 1.4 Triar cada sobrevivente relevante (teste novo cobrindo o comportamento, equivalência documentada ou justificativa) até a meta de 80% ou com dispensa registrada em `verification.md` — GREEN. Verificação: score final + triagem registrados. Execução: triagem por categoria em `verification.md` — (a) `errors.ts` 50%→100% com teste dos 4 códigos estáveis; (b) controller 36,84%→68,42% com corpo exato no teste HTTP + caso sem treatment; (c) whitespace-only em id/slotId/phone fechado com testes de fronteira; (d) equivalentes documentados: spread `undefined` vs ausente (JSON descarta), texto de mensagem UX (não assertado por decisão), pré-checagem defense-in-depth (a guarda do Domain cobre), spread de `notes` nunca exposto na resposta, leitura `toBookingDomain` sem chamador de produção

## 2. Stryker no contracts (test-first)

- [x] 2.1 Executar `pnpm --filter contracts exec stryker --version` e constatar que falha (binário inexistente) — RED. Verificação: saída de erro colada na task. Execução: `Command "stryker" not found` (RED real)
- [x] 2.2 Instalar as mesmas deps, criar `contracts/stryker.config.mjs` (escopo `src/scheduling/**`, exclusões e thresholds iguais) e script `mutation`; `stryker --version` passa — GREEN. Verificação: binário resolve e config carrega. Execução: `stryker --version` → 10.0.0; command runner pelo mesmo achado do backend (vitest 5); `concurrency: 2` (testes puros, sem banco)
- [x] 2.3 Executar o mutation nos schemas de scheduling e registrar score + tempo em `verification.md` — GREEN. Verificação: relatório completo no registro. Execução: **88,89%** na 1ª medição (16/18, 15s)
- [x] 2.4 Triar sobreviventes como na task 1.4 — GREEN. Verificação: score final + triagem registrados. Execução: dois gaps reais encontrados e fechados com testes — telefone curto com pontuação (`"(11) 1234"` passava) e fronteira de exatamente 10 dígitos (`>= 10` vs `> 10`); **score final 100%** (18/18, 14s)

## 3. Registro na seção 14 e verificação

- [x] 3.1 Adicionar à seção 14 de docs/07 o registro da sessão do Agendamento (4 padrões observados, texto curto, agnóstico de modelo, sem nomear IA) — exceção docs/07 §4 (documentação sem comportamento; verificação por releitura). Verificação: releitura confirma os 4 padrões e a conformidade com a regra de forma. Execução: 4 padrões adicionados (itens 4–7: grupos por camada com revisão entre eles; prova negativa real; revisão de segurança formal mesmo quando "parece" coberto; emenda de design em voo); `grep` por nomes de modelo/fornecedor → vazio
- [x] 3.2 Rodar gates nos arquivos tocados (`lint`, `format`, `typecheck`), auditoria sem high/critical com as novas deps, `pnpm exec openspec validate --all` e registrar `verification.md` — exceção docs/07 §4 só para a escrita dos registros; gates são executáveis. Verificação: gates verdes + `verification.md` com scores, triagem e tempos. Execução: gates completos verdes (lint/format/typecheck/test/build; contracts 55 testes, backend 54, frontend 109); auditoria sem high/critical; `.stryker-tmp/` e `reports/` ignorados em lint/format/git nos dois workspaces (achado: o sandbox do Stryker era varrido pelo lint durante a execução); `validate --all` registrado no archive

## 4. Frente B — checklist estrutural (documentação, exceção §4)

- [x] 4.1 Constatar ausência por `grep` ("Checklist obrigatório para módulo de backend novo" retorna vazio no docs/07) e escrever a seção 16 (5 itens + nota de citação em prompts futuros) — exceção docs/07 §4 (documentação sem comportamento; verificação por releitura). Verificação: releitura confirma os 5 itens, a nota de citação e a ausência de nomes de modelo/fornecedor. Execução: grep vazio antes; seção 16 criada (linha 232); grep por nomes de modelo/fornecedor → vazio
- [x] 4.2 Constatar ausência por `grep` (bloco condicional ausente no template) e estender `.github/pull_request_template.md` com a seção condicional de módulo novo, sem alterar o checklist geral — exceção docs/07 §4 (verificação por releitura do template renderizado). Verificação: releitura confirma o bloco condicional e o checklist geral intacto. Execução: bloco "Checklist de módulo de backend novo (só se este PR cria um módulo de backend)" adicionado após o checklist geral, que permaneceu intacto

## 5. Achado durante o apply — config do frontend (fora do escopo, registrado)

- [x] 5.1 Registrar como defeito descoberto (não corrigir nesta change): `pnpm --filter frontend mutation` **nunca funcionou** — falha de discovery do plugin vitest no layout pnpm (`Cannot find TestRunner plugin "vitest"`), e mesmo corrigido cairia no mesmo problema do vitest 5. Evidência e recomendação de follow-up em `verification.md` e no PR — exceção docs/07 §4 (registro). Verificação: evidência colada no registro. Execução: probe `--mutate lib/booking.ts` no frontend aborta com o erro de plugin; follow-up recomendado (aplicar plugins + command runner no frontend, change próprio)
