## 1. Hook pre-commit (RED→GREEN executável — sem exceção docs/07 §4, porque o hook é comportamento executável real)

- [x] 1.1 RED: criar arquivo scratch `frontend/lib/__tests__/hook-proof.tmp.ts` com um erro corrigível automaticamente (formatação fora do prettier) e um erro não-corrigível (variável indefinida, ex. `no-undef`), stagear SÓ o scratch (`git add` com pathspec, sem tocar em mais nada), commitar e verificar que o commit PASSA (prova que hoje nada barra lint/format localmente); em seguida desfazer o commit de prova (`git reset HEAD~1`, mantendo o scratch no disco para as tasks seguintes) e verificar que a árvore volta ao estado anterior — RED REAL comprovado: commit `01903b5` criado com erro de lint + formatação suja, exit 0 (saída registrada em `verification.md`); após `git reset HEAD~1`, só o scratch permanece untracked
- [x] 1.2 GREEN (caso corrigível): instalar `husky` + `lint-staged` como devDependencies da raiz (`pnpm add -D -w`, com `prepare: husky`), criar `.husky/pre-commit` e `lint-staged.config.mjs` na raiz (forma de função com normalização de caminhos, `eslint --fix` + `prettier --write` via `pnpm --filter frontend exec`); preparar scratch SÓ com o erro corrigível, stagear SÓ o scratch, commitar e verificar que o hook corrige o arquivo e o commit passa com o conteúdo corrigido (conferir no diff do commit); em seguida desfazer o commit de prova (`git reset HEAD~1`) sem remover os arquivos reais do hook/config (ainda unstaged) — GREEN REAL comprovado: commit `2f14e56` criado com o conteúdo CORRIGIDO no diff (espaçamentos normalizados), hook rodou `eslint --fix` ✔ + `prettier --write` ✔ e re-stageou; ajuste no apply com a prova como critério (design risco nº 2): lint-staged v17 exige função por glob-key (não objeto retornado por função de topo) e não roda comandos via shell (`cd` → ENOENT), então cada tarefa começa em binário real com `pnpm -C frontend exec`; arquivos reais do hook/config permanecem unstaged após o reset
- [x] 1.3 GREEN (caso não-corrigível + limpeza): preparar scratch SÓ com o erro não-corrigível, stagear SÓ o scratch, tentar commitar e verificar que o hook falha (exit ≠ 0) e o commit é barrado (nada commitado); em seguida remover o scratch do disco e do stage e verificar que `git status` está limpo de scratch (só restam os arquivos reais do hook/config/docs para a task 1.4) — GREEN REAL comprovado: hook falhou com `react/no-unescaped-entities` (3 errors, exit 1) e o commit foi barrado (HEAD permaneceu em `5eb6f9e`); ajuste do erro escolhido com a prova como critério (diagnóstico real registrado): na config do projeto `no-unused-vars` é warning, e `no-debugger`/`no-dupe-keys`/`rules-of-hooks` não dispararam como erro — o erro não-corrigível real usado foi apóstrofo/aspas não escapadas em JSX (error, sem autofix); scratch `.tsx` removido do stage e do disco, árvore limpa
- [ ] 1.4 Comitar de verdade os arquivos do hook (`package.json` raiz, `pnpm-lock.yaml`, `.husky/pre-commit`, `lint-staged.config.mjs`) em commit semântico `chore(...)` e verificar que um novo commit qualquer na branch dispara o hook (saída do lint-staged visível) e passa

## 2. Documentação e verificação final

- [ ] 2.1 Atualizar `docs/engineering/07-workflow-de-engenharia.md` junto à Definition of Done (seção 6) com a nota das três camadas de defesa — (1) pre-commit local: feedback rápido, burlável via `--no-verify`, nunca fonte da verdade; (2) CI: gate autoritativo em push/PR; (3) branch protection: merge bloqueado sem CI verde — e verificar que o texto declara explicitamente que o pre-commit NÃO substitui as outras duas (task só de documentação, sem comportamento: exceção docs/07 §4 registrada aqui)
- [ ] 2.2 Rodar quality gates (`lint`, `format`, `typecheck`, testes com cobertura >80%, `build`) e `openspec validate`, registrar em `verification.md` a prova executável das tasks 1.1–1.3 (RED passando sem hook; GREEN corrigindo e barrando), a dispensa de gatilho §7 (tooling puro, sem entrada de usuário/dado sensível — registro obrigatório mantido para o archive) e a revisão com `code-review-and-quality`, e verificar que tudo está verde
