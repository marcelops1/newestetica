## 1. Contrato/schema e submissão mockada (TDD estrito — docs/07 §13: unitários com edge cases + contrato/schema com apoio da skill `api-and-interface-design`)

- [x] 1.1 RED: escrever `frontend/lib/__tests__/contact.test.ts` testando o contrato da fronteira UI ↔ submissão mockada (payload válido com nome/contato/mensagem passa no schema; payload malformado — campo ausente, tipo errado em nome/contato/mensagem, contato fora dos dois formatos — retorna erro acolhedor sem vazar detalhes) e o comportamento de `submitContactRequest` (sucesso, `forceError` com mensagem sem culpa, nenhuma chamada de rede via spy de `fetch`) e verificar que a suite falha (módulo ainda inexistente) — RED comprovado: `Cannot find module '../contact'`
- [x] 1.2 GREEN: implementar `frontend/lib/contact.ts` (`ContactInput`/`ContactResult`, schema/validador do payload desenhado com apoio da skill `api-and-interface-design`, `submitContactRequest` espelhando `submitQuoteRequest`: 100% local, delay mockado, `forceError` só para demonstração/testes) e verificar que o teste da task 1.1 passa, os testes existentes (`booking.test.ts`, `quote.test.ts`, `data.test.ts`) seguem passando e a cobertura fica acima de 80% (meta: 100% em `lib/contact.ts`, como em `quote.ts`) — GREEN comprovado: 42/42 testes; cobertura global 92,02%/87,75%/97,61%/91,53% (>80%); `validateContactFields` (58–73) coberto na task 2, meta 100% em `contact.ts` na task 2.2

## 2. Segurança OWASP de entrada (task dedicada — gatilho docs/07 §7: formulário = entrada de usuário real; docs/07 §13: segurança orientada a OWASP)

- [x] 2.1 RED: escrever `frontend/lib/__tests__/contact-security.test.ts` com validação das duas faces do campo flexível (e-mail válido passa, WhatsApp com ≥10 dígitos passa, texto que não é nenhum dos dois gera erro acolhedor; nome <2 e mensagem vazia geram erro por campo) e casos maliciosos — `<script>alert(1)</script>`, `<img src=x onerror=...>`, `&`, aspas em nome/mensagem, mensagem longa — exigindo tratamento como texto (sem execução/interpretação de HTML), e verificar que os novos testes falham — RED comprovado: 1 falha comportamental (fronteira aceitava nome de 1 caractere — o gate de campos na submissão não existia); os testes de `validateContactFields` passaram de primeira porque a função já saiu no GREEN 1.2 — lacuna de teste, não de implementação (mesmo caso da correção de cobertura do orçamento, docs/07 §4)
- [x] 2.2 GREEN: implementar o hardening na validação/escaping de `lib/contact.ts` (`validateContactFields` com as duas faces do contato + `parseContactInput` rejeitando tipo errado na fronteira, reaproveitados pela página) e verificar que os testes da task 2.1 passam, que `grep -r "innerHTML\|dangerouslySetInnerHTML" frontend/` retorna vazio na nova superfície, e que a cobertura de `lib/contact.ts` chega a 100% nas 4 métricas (statements, branches, functions, lines) — GREEN comprovado: 29/29 testes de contato, grep vazio, cobertura 100% (141/141 stmts, 103/103 branches, 42/42 funcs, 133/133 lines — todas as libs); hardening: gate de `validateContactFields` ligado à fronteira de `submitContactRequest`

## 3. Página /contato e fluxo crítico (lógica testada nas seções 1–2; montagem de UI por gates — sem testing-library no projeto, lacuna registrada no design)

- [x] 3.1 Implementar `frontend/features/contact/ContactPage.tsx` (client component padrão `QuotePage`: formulário nome/contato flexível/mensagem reaproveitando microcopy, classes e nota LGPD; bloco institucional via `getContactInfo()` com endereço/horário fictícios, sem número real novo; 4 estados ocioso/enviando/sucesso/erro com dados preservados e nova tentativa; confirmação acolhedora sem prometer prazo fechado; mobile-first 16px+/toque 44px+, `aria-invalid`/`aria-describedby`) e `frontend/app/contato/page.tsx` com metadata em tom acolhedor, e verificar que lint, format, typecheck, testes e build passam com `/contato` prerenderizada — lint 0 erros (1 warning pre-existente em `stryker.config.mjs`, fora do escopo), format ok, typecheck ok, 29/29 testes de contato, build com `/contato` estática
- [x] 3.2 Verificar o fluxo crítico preencher → enviar → confirmação pelos gates disponíveis (build prerenderiza a rota, typecheck, testes da lib verdes, revisão manual do fluxo sucesso/erro com dados preservados e das duas faces do campo flexível) e verificar que nenhum fetch/XHR foi introduzido (spy de `fetch` nos testes + `grep` por `fetch(`/`axios` na nova superfície, exceto o spy) — grep vazio na nova superfície; fluxo revisado: erro preserva mensagem (formulário não remontado), sucesso é confirmação estática sem ecoar dados, placeholder mostra os dois formatos do campo flexível

## 4. Gates, revisão de segurança e backlog (Definition of Done docs/07 §6; revisão `security-and-hardening` obrigatória pelo gatilho §7)

- [x] 4.1 Rodar quality gates (`lint`, `format`, `typecheck`, testes com cobertura >80% — meta 100% em `lib/contact.ts`, `build`) e `openspec validate`, registrar em `verification.md` a revisão com `security-and-hardening` contra `docs/security/03-seguranca.md` (entrada de usuário, XSS/escaping, LGPD, sem dado real) e a revisão com `code-review-and-quality`, com as dispensas da seção 13 (mutation, E2E, carga) justificadas como no design, e verificar que tudo está verde — gates verdes (82/82 testes, cobertura 100% nas 4 métricas, build com `/contato` estática); revisões registradas em `verification.md`
- [ ] 4.2 Adicionar o Use Case 1.8.2 — Enviar mensagem pelo site (Feature 1.8, status refletindo o apply, sem alterar o UC 1.8.1 nem nenhum outro Use Case) em `docs/product/08-backlog-produto.md` e verificar que o trecho do UC 1.8.2 reflete o que foi entregue neste change
